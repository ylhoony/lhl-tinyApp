<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <header>
    <% include ./partials/_header %>
  </header>

    <div>

      Short URL: <%=shortURL%>

      <form method='post' action='/urls/<%=shortURL%>/update'>

        <input id="longURL" type="text" name="longURL" value='<%=userData[shortURL]%>' style='width: 300px'>
        <% if (userId === undefined) { %>
          <button disabled>Update</button>
        <% } else { %>
          <button>Update</button>
        <% } %>
      </form>

    </div>



</body>
</html>